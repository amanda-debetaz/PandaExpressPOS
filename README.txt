Ignore node_modules files when pushing to github. Use git.ignore for this functionality.

Push package.json and package-lock.json to let user know what they need to download.

For Images:
Their name structure is item.jpg, replacing the spaces (if any) in the item with a hyphen and lowercasing everything (Orange Chicken is orange-chicken.jpg)