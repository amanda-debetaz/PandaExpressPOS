<!DOCTYPE html>
<html>
<head>
  <title>Kitchen Queue</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    h1 { text-align: center; }
    .order { border: 1px solid #ccc; padding: 10px; margin-bottom: 15px; border-radius: 8px; }
    .order-header { font-weight: bold; margin-bottom: 5px; }
    .line-item { margin-left: 20px; }
    .options { font-style: italic; color: #555; }
  </style>
</head>
<body>
  <h1>Kitchen Queue</h1>

  <% if (orders.length === 0) { %>
    <p>No queued orders.</p>
  <% } else { %>
    <% orders.forEach(order => { %>
      <div class="order">
        <div class="order-header">
          Order #<%= order.orderId %> - Status: <%= order.status %> - <%= order.dineOption %>
        </div>
        <% if (order.notes) { %>
          <div>Notes: <%= order.notes %></div>
        <% } %>
        <div>Placed At: <%= new Date(order.placedAt).toLocaleString() %></div>

        <ul>
          <% order.items.forEach(item => { %>
            <li class="line-item">
              <%= item.qty %> x <%= item.menuItem.name %>
              <% if (item.options && item.options.length > 0) { %>
                <span class="options">
                  (Options: <%= item.options.join(", ") %>)
                </span>
              <% } %>
            </li>
          <% }) %>
        </ul>
      </div>
    <% }) %>
  <% } %>
</body>
</html>
